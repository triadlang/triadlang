# python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/
venv/
.venv/

# native build artifacts: the shared library is built locally so it never
# carries a machine-specific cuda runtime dependency into the repo. only the
# Makefile and sources are tracked; run `make` in native/c/ to produce them.
native/c/libtriad_rt.so
native/c/obj/
*.o

# triad caches and compiled output
__triadcache__/
triad_modules/

# editor
.vscode/
.idea/

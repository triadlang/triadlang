import fs;

let path = fs.join(fs.tempdir(), "triad_test.txt");
fs.write_text(path, "Hello from TriadLang file I/O!");

let content = fs.read_text(path);
print(content);

print("exists: " + str(fs.exists(path)));
print("missing: " + str(fs.exists(fs.join(fs.tempdir(), "does_not_exist.txt"))));
